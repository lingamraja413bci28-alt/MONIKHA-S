# Read the initial set A
set_a = set(map(int, input().split()))

# Read the number of other sets
n = int(input())

is_strict_superset = True

for _ in range(n):
    # Read each of the other sets
    other_set = set(map(int, input().split()))
    
    # Check if set_a is a strict superset of other_set
    # The '>' operator in Python sets checks for a strict superset
    if not (set_a > other_set):
        is_strict_superset = False
        break

# Output the final result
print(is_strict_superset)
